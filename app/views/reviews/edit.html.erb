<div class="col-md-6 col-md-offset-3">
    <div class="filme-form">
        <h1>Adicionar Review</h1>
       <h1> Editar Review </h1>

    <%= simple_form_for([@filme, @review]) do |f| %>
        <%= f.input :nota %>
        <%= f.input :comentario %>
        <%= f.button :submit, 'Editar Review'%>
    <% end %>
    </div>
</div>