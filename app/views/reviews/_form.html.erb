<%= simple_form_for([@filme, @filme.reviews.build]) do |f| %>
    <%= f.input :nota, label: "Nota (0 - 10)" %>
    <%= f.input :comentario %>
    <%= f.button :submit, :class => 'btn-custom2' %>
<% end %>